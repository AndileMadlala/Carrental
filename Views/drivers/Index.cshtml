@model IEnumerable<carrentalservice.Models.driver>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Drivername)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.costPerKilo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Image2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImageType2)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Drivername)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.costPerKilo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Image2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageType2)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.DriverID }) |
                @Html.ActionLink("Details", "Details", new { id = item.DriverID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.DriverID })
            </td>
        </tr>
    }

</table>
@Html.ActionLink("Book drivers", "DriverAvail")
